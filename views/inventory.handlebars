<div class="page-{{title}}" ng-controller="inventoryController">

	<input type="text" ng-model="test">
	<ul>
		<li ng-repeat="x in inventory | filter:test | orderBy:'item'">
			<[x.item]>
		</li>
	</ul>
</div>